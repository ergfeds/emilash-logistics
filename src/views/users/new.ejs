<% layout('layouts/admin') %>

<div class="row mb-4">
	<div class="col-12">
		<h1 class="h3 mb-0">👤 Create New User</h1>
		<p class="text-muted">Add a new admin or staff user</p>
	</div>
</div>

<div class="card">
	<div class="card-header">
		<h5 class="mb-0">User Information</h5>
	</div>
	<div class="card-body">
		<form method="post" action="/admin/users" class="row g-3">
			<div class="col-md-6">
				<label class="form-label">👤 Full Name *</label>
				<input name="name" class="form-control" required value="<%= (form && form.name) || '' %>" placeholder="John Doe">
			</div>
			<div class="col-md-6">
				<label class="form-label">📧 Email Address *</label>
				<input type="email" name="email" class="form-control" required value="<%= (form && form.email) || '' %>" placeholder="john@example.com">
			</div>
			<div class="col-md-6">
				<label class="form-label">🔒 Password *</label>
				<input type="password" name="password" class="form-control" required placeholder="Minimum 6 characters">
			</div>
			<div class="col-md-6">
				<label class="form-label">🏷️ Role *</label>
				<select name="role" class="form-select" required>
					<option value="admin" <%= (form && form.role === 'admin') ? 'selected' : '' %>>Admin</option>
					<option value="staff" <%= (form && form.role === 'staff') ? 'selected' : '' %>>Staff</option>
				</select>
			</div>
			<div class="col-12">
				<div class="alert alert-info">
					<strong>👮 Admin:</strong> Full access to all features including settings and user management<br>
					<strong>👤 Staff:</strong> Limited access to shipment management only
				</div>
			</div>
			<div class="col-12">
				<div class="d-flex justify-content-end gap-2">
					<a href="/admin/users" class="btn btn-outline-secondary">❌ Cancel</a>
					<button type="submit" class="btn btn-primary">✅ Create User</button>
				</div>
			</div>
		</form>
	</div>
</div>

